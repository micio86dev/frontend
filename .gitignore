# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example

# Playwright test results
test-results/
playwright-report/

# Coverage
coverage/

# MediaPipe proctoring binaries — build artifacts, not source.
# Provisioned and verified by `bun run proctor:assets` (see .gitattributes).
public/proctor/wasm/
public/proctor/*.task
public/proctor/*.tflite

# Local skill-registry cache (tooling artifact, never project code)
.atl/
